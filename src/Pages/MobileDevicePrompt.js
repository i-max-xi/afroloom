import React from 'react';

const MobileDevicePrompt = () => {
  return (
    <div className="mobile-prompt" style={{display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100vh'}}>
      <p>Please use a desktop device to access this website.</p>
    </div>
  );
};

export default MobileDevicePrompt;
